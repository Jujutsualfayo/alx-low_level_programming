#include <stdio.h>

/**
 * main -This is the main functionof the second task
 * Return: Always (Success)
 */

int main(void)
{
char c;
for (c = 'a'; c <= 'z'; c++)
putchar(c);
putchar('\n');
return (0);
}
